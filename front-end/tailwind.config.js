/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // 👈 ensures dark mode works via class on <html>
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
    
  ],
  theme: {
    extend: {},
  },
  plugins: [
    
  ],
}
